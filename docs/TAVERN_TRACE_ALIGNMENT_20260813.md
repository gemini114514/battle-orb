# 酒馆请求基准对齐记录（2026-08-13）

## 基准与范围

本次对齐基准为本机保留的两份只读黑盒导出：

- `C:\Users\fengx\Downloads\vertex-exchange-trace-20260813-085326.json`：酒馆实际发出的两轮会话相关请求，以及一次商城刷新请求。
- `C:\Users\fengx\Downloads\轮回战场-黑盒-2026-08-13T08-41-49-997Z.json`：旧网页版的对应请求，作为差异定位资料。

范围限定为正文对话的提示词组装，以及个人商店刷新的提示词和模型输出接收。不以本地规则补写、重定价、重定级或修正模型的商城结果。

## 已完成对齐

### 正文对话

- 补齐酒馆卡使用的宏：`trim`、`addvar`、`getvar`、`lastUserMessage`、`lastMessage`、`user`、`char`、旧式 `<user>/<char>` 与骰子宏。
- 宏在每次构建提示词时重新初始化；`lastUserMessage` 取本次可见历史的最后一条用户输入。
- 若主预设以 `lastUserMessage` 内联互动输入，历史末条用户消息不再重复追加。
- 对 Maya 这类“重置前导 + 大型上下文”的预设，保留酒馆请求的角色分布：重置前导为 `system`，卡片/上下文为 `user`，而非把全部内容压成一个 `system` 消息。
- 为了不偏离原卡，互相好感度等网页版扩展数据不再额外注入正文主提示词；它们仍保存在本地状态中。

### 个人商店

- 刷新请求改为两条消息：酒馆卡渲染后的商城 `system` 提示词 + 当前玩家状态与需求的 `user` 提示词；不叠加 AIRP 正文预设。
- `system` 的静态头、四条世界书条目的顺序、EJS 渲染结果、空行和生成约束均依据真实酒馆请求保留。
- 在使用同一玩家身份（`艾莉丝`）渲染时，生成的商城 `system` 与基准请求逐字符相同：**16,467 字符，完全一致**。
- 使用记录中的同一主角状态和需求时，商城 `user` 消息同样逐字符相同：**1,421 字符，完全一致**。原请求内示例的品质、属性与价格组合也保留为模型锚点，不被改写成独立端的本地示例。
- 接受酒馆常见的 YAML 输出（也兼容 JSON），并保留血统、形态、技能、装备、道具、升级六类商品；新增“形态”分类以避免原输出丢失。
- 兼容模式下模型输出直接入库，仅做结构归一化，不再由本地代码静默改写价格、品质、伤害、效果或数值。

## 已验证

- 商城系统提示词与真实酒馆记录逐字符比对：通过。
- 预设宏、Maya 角色顺序与首轮输入内联的浏览器集成测试：通过。
- 商城兼容请求、YAML 解析、形态保留、模型结果不被本地“修复”的集成测试：通过。
- 常规正则、开局 MVU 等既有回归测试：通过。

## 保留的冲突，等待决定

1. 原始商城提示词中明确写有“商城视野 = 当前层级 + 1 阶（Ⅰ=F，Ⅱ=E……）”。此前需求又要求生命层级与 F–SSS 不做绝对转换。这是同一基准内的业务冲突。本次为了酒馆兼容性，逐字保留原提示词，未自行改写；需要决定是继续忠实使用该卡规则，还是为新版卡建立一条经过确认的替代规则。          
A:商店部分保持层级与字母转换的规则，尽可能对齐原卡平衡表现。
2. 真实记录的商城调用为 `gemini-3.5-flash`、`temperature: 0.7`；独立端仍按“商店 AI”配置档选择连接、模型和采样参数。若需要请求层也完全锁定酒馆值，需要明确是否覆盖用户配置。
A:影响不大，不管这个。
3. 真实记录还包含“记忆提取”和“世界演化”后台调用。本次范围仅含正文与商城，因此没有新增这两条后台调用；若要求端到端请求序列完全一致，需要另行接入并指定其触发时机。
A:插件部分这里不管。
4. 原卡的关系数据以主角关系为主，独立端已经保存了实体间好感度扩展。为了正文兼容，该扩展目前不进入主提示词。后续若要写入提示词，需要确认原卡格式之外的字段协议。
A:先不管这部分

新增待解决问题 玩家的装备与道具操作逻辑需要尽可能对齐酒馆卡的悬浮球操作，尤其是装备拿取卸下槽位限制什么的提示等。显示物品与道具的slot渲染需要完全参考悬浮球的着色逻辑，确保层级有对应slot颜色显得更直观。


5. 正文记录保存的是已经合成的请求，而非生成该请求的原始 OAI 预设文件。独立端已按记录实现角色顺序、宏、World Info 槽位与输入内联；要做到正文固定文本的逐字符重建，仍需提供产生该记录的同一份原始 AIRP/OAI JSON（当前的“明月秋青写卡预设”并不是这份记录所示的同一预设）。
A:不管这部分

## 已确认的后续落地（2026-08-13）

- 商城路径已采用原卡的视野表：Ⅰ=F、Ⅱ=E……；该换算仅在商城草案与旧兼容商城提示词中存在，绝不写入正文提示词、角色档案或 MVU 的生命层级字段。基础视野之外最多生成少量高一阶诱惑商品。
- 装备与道具页面已改为同悬浮状态栏协议：状态 `0` 为背包、`1` 为战术栏、`2` 为仓库；双武器/双饰品满额会拒绝，单槽会自动脱下旧件，道具战术栏上限为五件，世界遗物不进入常规穿戴/存取流程。
- 品质着色采用悬浮球色板。D 级为 `#22c55e` 绿色左侧边框与绿色等级牌；F 至 SSS 的边框与等级牌均按同一映射渲染。
- 装备、脱下、存放、取回、容量拒绝及标签切换都会写入本机黑盒，便于后续导出追溯。

## Izumi 0503 请求结构校准（2026-08-16）

以 `vertex-exchange-trace-20260815-153016.json` 的记录 0（酒馆）与记录 18/19（独立端）为结构基准，忽略仅影响用户身份名称的差异：

- `prompt_order` 现在按酒馆的 allow-list 解释。预设启用但未列入当前角色顺序的「过渡」与「💾通用主提示」不会再误发；旧 IndexedDB 预设启动时会一次性迁移。
- 支持 `squash_system_messages: true`：保留 382 字的首个 system 锚点，首个 assistant 示例前补齐 `[Start a new chat]`，其余系统上下文合并为 user 侧上下文，保留 assistant 示例边界。
- `{{lastUserMessage}}` 内联时不会把历史末条用户重复塞入；若生成边界仍落在 assistant，则补一个合法 user 回合，避免 Gemini 拒绝尾部 model/system 结构。
- BattleDeclaration 规则并入现有 system 锚点，不再在最后一个用户/assistant回合之后追加 system 消息。
- SSE 中的上游 `error` 事件现在会原样进入生成失败提示与黑盒记录，不再表现为空回复。

验证命令：`npm run test:prompt-alignment`、`npm run test:ui`。
